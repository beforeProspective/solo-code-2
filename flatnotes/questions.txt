问题1
背景介绍：前端用于编辑笔记的组件client/views/Note.vue中，设计了草稿保存与自动保存机制。当用户编辑内容发生变化时，会调用tokenStorage.js中的isCurrentTokenStored来判断是否将草稿内容写入本地。
1.详细分析Note.vue中的saveDraft和loadDraft方法，它是如何根据isCurrentTokenStored返回的布尔值来选择使用localStorage还是sessionStorage来存取草稿的？
2.当用户的登录令牌过期且自动保存的计时器触发时，Note.vue是如何通过API调用接口拦截捕获到该状态，并在清除本地草稿的同时将页面安全导向登录界面的？
3.若用户未登录或仅有只读权限，Note.vue是如何结合全局配置与计算属性canModify来禁用草稿保存与自动保存机制的？
最终的回答和分析以md文件的形式输出，保存在当前根目录下。

问题2
背景介绍：客户端路由守卫定义在client/router.js中，而网络请求的配置和拦截器实现于client/api.js中，两者均依赖tokenStorage.js进行登录凭证生命周期管理。
1.详细分析router.beforeEach路由守卫在用户进行页面导航时，是如何通过调用api.js中的authCheck函数来核实登录状态的，且该守卫是如何防止无限重定向循环的？
2.当api.js中的Axios拦截器拦截到非api/token的请求时，它是如何通过tokenStorage.js中的getStoredToken获取令牌并将其注入Authorization请求头部的？
3.如果在API请求过程中服务端返回了401未授权错误，api.js中的apiErrorHandler是如何通过Vue虚拟路由器实例捕获当前路由的fullPath并将其作为查询参数redirect传递给登录路由的？
最终的回答和分析以md文件的形式输出，保存在当前根目录下。

问题3
背景介绍：首页定制展示与搜索结果渲染分别由client/views/Home.vue和client/views/SearchResults.vue实现，两者的逻辑均受到globalStore.js中存储的全局配置项的调控。
1.详细分析Home.vue在组件挂载阶段时，是如何读取Pinia存储中useGlobalStore的全局配置来决定是否隐藏快速访问卡片以及应用何种默认搜索过滤词的？
2.当用户在SearchResults.vue中触发搜索或者修改排序规则时，组件是如何将搜索请求与排序参数同步更新到Vue的路由查询参数中，从而支持浏览器历史前进与后退状态保留的？
3.分析这两个视图组件在执行接口调用时，是如何在模板中绑定加载状态LoadingIndicator和错误消息通知PrimeToast来提升用户交互体验的？
最终的回答和分析以md文件的形式输出，保存在当前根目录下。

问题4
背景介绍：文件笔记存储的核心业务逻辑位于server/notes/fileSystem/fileSystem.py中，并在FastAPI后端的主文件server/main.py中注册为路由端点，直接操纵底层操作系统的Markdown文件。
1.详细分析fileSystem.py中的syncIndexWithRetry方法，在面临多线程高并发写操作产生锁冲突时，它是如何通过捕获LockError进行最多8次且每次0.25秒延迟的重试退避的？
2.当并发请求导致重试耗尽并抛出锁异常时，main.py中的FastAPI路由会如何响应请求，对调用方返回何种状态码和错误描述信息？
3.分析在调用笔记更新接口时，后端是如何先判定文件名合法性，再调用系统函数重命名，最后在Whoosh写锁定前更新对应磁盘文件以确保数据物理落盘的？
最终的回答和分析以md文件的形式输出，保存在当前根目录下。

问题5
背景介绍：附件管理模块在server/attachments/fileSystem/fileSystem.py中实现，而文件名校验由通用工具文件server/helpers.py提供，最终通过FastAPI的静态目录响应进行分发。
1.分析FileSystemAttachments在接收上传附件时，是如何利用helpers.py中的isValidFilename函数防止跨目录遍历恶意字符绕过的，请描述该防御的黑名单范围？
2.追踪附件新建方法，在上传目标文件名已经存在于attachments目录时，底层是如何通过捕获FileExistsError并调用datetimeSuffixFilename动态添加时间戳后缀来成功保存的？
3.分析当用户通过/attachments/文件名端点发起下载请求时，FastAPI是如何在主路由中利用FileResponse将磁盘上的静态附件文件流式传输给客户端的？
最终的回答和分析以md文件的形式输出，保存在当前根目录下。

问题6
背景介绍：后端应用的启动校验与配置解析在server/globalConfig.py中实现，并通过定制的系统日志记录器server/logger.py以及全局状态消息定义文件server/apiMessages.py进行协同运行。
1.详细分析globalConfig.py中的getEnv函数，它是如何在配置项缺失或者类型强制转换失败时，输出logger.py定义的日志记录并强行调用sys.exit终止进程启动的？
2.探讨当系统运行时抛出不合法的笔记标题或错误的令牌参数时，后端是如何统一调用apiMessages.py中预设的字面值常数作为异常错误详情返回的？
3.分析后端日志模块logger.py的级别定义与格式输出设计，在并发环境下多模块同时调用logger.debug或logger.error输出日志时，是如何保障日志文件写入的安全性的？
最终的回答和分析以md文件的形式输出，保存在当前根目录下。

问题7
背景介绍：用户登录过程涉及前端client/api.js发送凭证，以及后端FastAPI的本地身份认证模块server/auth/local/local.py中的双因子拼接校验与令牌状态同步。
1.当前端开启双因子动态验证TOTP时，api.js的getToken方法是如何将密码与动态验证码连接并发送给后端的？后端在验证时又是如何进行解密与防重放判定的？
2.分析后端生成的JWT令牌中所携带的过期载荷，以及在FastAPI的验证注入方法LocalAuth.authenticate中，如何做到同时兼容HTTP头部的Bearer凭证和请求Cookie来提取该凭证的？
3.分析当用户在前端退出登录时，前端如何配合tokenStorage.js更新Cookie过期时间为1970年，以及服务端是如何响应并废弃客户端此后发起的旧令牌状态校验的？
最终的回答和分析以md文件的形式输出，保存在当前根目录下。

问题8
背景介绍：笔记检索流程连接着前端搜索结果展示组件client/views/SearchResults.vue与后端server/notes/fileSystem/fileSystem.py的索引查找及片段截取接口。
1.当用户在SearchResults.vue中发起关键字搜索请求时，后端是如何在Whoosh索引中根据查询内容是否包含双引号来分别决定是否在tags字段中进行匹配的？
2.详细分析后端命中查询结果后，针对标题的WholeFragmenter和针对正文内容的ContextFragmenter分别生成了怎样的命中高亮片段，并简述两者的定位原理？
3.前端拿到后端返回的包含高亮标签的字段后，由于Markdown文本中包含这些动态高亮，前端是如何在Note.vue与SearchResults.vue中安全渲染富文本而规避XSS注入攻击的？
最终的回答和分析以md文件的形式输出，保存在当前根目录下。

问题9
背景介绍：笔记重命名操作要求前端client/views/Note.vue编辑页的交互状态，与后端笔记存储库server/notes/fileSystem/fileSystem.py中的文件重命名事务进行强一致的校验。
1.当用户在Note.vue中更改标题为新名称并保存时，后端fileSystem.py在重命名底层文件前，是如何通过检测文件存在性来判断命名冲突并抛出FileExistsError的？
2.当后端抛出上述文件已存在异常时，FastAPI路由端点是如何捕获该异常并将其转换为HTTP状态码409响应发送给前端的？
3.前端api.js的updateNote在收到409状态后，Note.vue的保存回调函数是如何拦截此特定响应，并向用户弹出“标题已存在”的提醒，从而引导用户重新输入而不是强行退回到只读状态的？
最终的回答和分析以md文件的形式输出，保存在当前根目录下。

问题10
背景介绍：路径前缀适配涉及后端启动时修改client/dist/index.html文件，与前端路由client/router.js所依赖的base标签基准路径在网络层与浏览器层的协同解析。
1.详述FastAPI服务初始化时，server/helpers.py中的replaceBaseHref是如何通过正则表达式将动态配置的FLATNOTES-PATH-PREFIX注入到前端打包模板的base标签中的？
2.分析在Vite打包后的SPA应用中，vue-router通过createWebHistory创建的路由历史，是如何借助HTML中的base标签所指定的URL前缀来实现静态API与动态路由跳转解析的？
3.如果前端发起的API异步请求（如获取配置文件端点api/config）因为前缀配置错误导致页面请求被后端静态文件服务app.mount错误拦截，浏览器侧会表现出何种网络错误及DOM渲染错误？
最终的回答和分析以md文件的形式输出，保存在当前根目录下。
